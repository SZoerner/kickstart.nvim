tags
test.sh
.luarc.json
nvim

spell/

# NOTE: `nvim-pack-lock.json` is intentionally tracked in this fork, so plugin
# revisions are pinned and reproducible - see :help vim.pack-lockfile
# (upstream `nvim-lua/kickstart.nvim` ignores it to avoid unneeded merge conflicts).

.DS_Store
